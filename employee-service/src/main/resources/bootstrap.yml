spring:
  profiles:    active:    - qa
  application:
    name: employee-service
  zipkin:
    base-url: http://localhost:9411/
    enabled: false
  cloud:    config:      uri:      - http://localhost:8888
      
eureka:
  client:
    service-url: 
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: false
    enabled: false