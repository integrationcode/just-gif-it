spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/integrationcode/springcloudconfig.git
            
server:
  port: 8888